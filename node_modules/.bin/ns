#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../npm-scripts/bin/ns" "$@"
  ret=$?
else 
  node  "$basedir/../npm-scripts/bin/ns" "$@"
  ret=$?
fi
exit $ret
